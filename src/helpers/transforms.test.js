describe("transformPlayers", () => {
  it("should order players into their associated teams", () => {});

  it("should align team IDs to team names", () => {});

  it("should remove players with no defined ffpg score", () => {});

  it("should return correct details on players", () => {});
});
