describe("Game", () => {
  test("playing a game round should show proceed button after play", () => {});

  test("playing a correct round should increment score", () => {});

  test("playing an incorrect round should retain current score", () => {});

  test("playing a round should never offer the same players again", () => {});

  /* INTEGRATION TESTS */
  //override the constant being used to quicken game
  test("playing a full successful game should allow for resetting", () => {});

  test("playing all available players should allow for resetting", () => {});
});
